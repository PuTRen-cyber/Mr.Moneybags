[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mr-moneybags"
version = "0.0.1"
description = "A minimal personal agent harness foundation for Mr.Moneybags and JIA."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["mr_moneybags*"]
namespaces = false
